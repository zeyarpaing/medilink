/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['unsplash.com', 'images.unsplash.com', 'source.unsplash.com', 'img.freepik.com'],
  },
  poweredByHeader: false,
};

module.exports = nextConfig;
